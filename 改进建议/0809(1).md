这版我仔细看了。**比上一版明显进步，而且你之前说的两个问题已经解决了一半。**尤其是“回答生硬”和“标题没传进去”，你确实已经开始往正确方向改了。

但我直说：**现在还不能算改完。**有几个地方会继续导致你截图里的那种“AI腔”和引用问题。

---

# 1. 最大的进步：回答风格已经改对方向了

你现在这一段：

> “让用户感觉是在和一个懂医学的朋友聊天，而不是在读医学论文摘要或免责声明。”

以及：

> “开头不要总用‘根据参考资料’”

> “避免‘首先…其次…另外…’的机械罗列”

> “不要每次回答都把‘以上内容仅供科普参考’顶在结尾”

这几个修改是**非常正确的**。

相比之前：

```text
严格基于资料
每条结论引用
必须免责
必须模板
必须标来源
```

现在已经开始给模型“说人话”的空间了。

### 但是，还有一个很大的隐患：

你下面的：

```python
QUESTION_TYPE_TEMPLATES
```

还保留着非常强的“模板腔”。

比如：

```text
## 症状描述类问题输出模板
1. 说明症状的可能原因
2. 描述症状的常见表现
3. 提示何时需要就医
4. 标注来源
```

以及：

```text
## 预防保健类问题输出模板
1. 列出预防措施
2. 说明关键注意事项
3. 强调定期体检的重要性
4. 标注来源
```



**这和你上面刚写好的“自然对话”是有冲突的。**

模型同时收到：

> “像朋友一样聊天”

和：

> “1、2、3、4”

它很可能还是会选择后者。

---

# 2. 我建议你把“输出模板”改成“回答重点”

这是我现在最建议你改的一处。

不要：

```text
## 症状描述类问题输出模板
1. 说明症状的可能原因
2. 描述症状的常见表现
3. 提示何时需要就医
4. 标注来源
```

改成：

```text
## 症状类问题回答重点
回答时根据参考资料自然组织内容，可以涉及：
- 这种症状通常有哪些表现
- 资料中提到的常见原因
- 什么情况下建议及时就医

不要强制按照以上顺序回答，也不要机械罗列。
根据用户的问题选择最相关的内容即可。
```

治疗：

```text
## 治疗/用药类问题回答重点
如果参考资料涉及治疗或用药：
- 只介绍资料中明确提到的内容
- 不提供具体药物剂量
- 不提供针对用户个人情况的治疗方案
- 用自然语言解释，不要写成说明书
```

这样模型会舒服很多。

---

# 3. 你现在“标题没有传进去”的问题，Prompt 这一层已经修好了

这个地方你改得非常对：

```python
title = meta.get("title", "")
source = meta.get("source", "未知")
category = meta.get("category", "")
publish_date = meta.get("publish_date", "") or meta.get("update_time", "")
url = meta.get("url", "")
```

然后：

```python
if title:
    header_parts.append(f"标题：{title}")
```

现在模型实际看到的是：

```text
【参考资料 1】
标题：XXX
来源：中华医学会
分类：科普与健康
发布时间：2026-04-21
URL：https://...
```

这一点已经比你上一版完整很多。

### 所以现在我要纠正之前对你的判断：

**如果前端最终仍然没有显示文章标题，那现在大概率已经不是 `prompt.py` 的问题了。**

而是下面两层之一：

```text
爬虫
 ↓
TXT
 ↓
加载 TXT → metadata   ← ★重点检查
 ↓
Chroma
 ↓
Retriever
 ↓
build_user_prompt()
```

你的 `prompt.py` 已经要求：

```python
meta.get("title")
```

但如果 Chroma 检索出来的：

```python
metadata
```

里面根本没有：

```python
"title": "xxx"
```

那么这里依然是：

```text
标题：
```

空的。

---

# 4. 所以你现在最应该检查的不是爬虫，而是“入库代码”

你的爬虫已经有：

```text
标题: xxx
来源: 中华医学会
URL: xxx
发布时间: xxx
分类: 科普与健康
```

而你现在 Prompt 要：

```python
metadata["title"]
metadata["source"]
metadata["url"]
metadata["publish_date"]
metadata["category"]
```

**两边必须一一对应。**

也就是：

| TXT  | Chroma metadata | Prompt         |
| ---- | --------------- | -------------- |
| 标题   | `title`         | `title`        |
| 来源   | `source`        | `source`       |
| URL  | `url`           | `url`          |
| 发布时间 | `publish_date`  | `publish_date` |
| 分类   | `category`      | `category`     |

你现在 Prompt 这一列已经改好了。

**下一步你把“TXT → LlamaIndex → Chroma”的代码给我，我就能判断标题到底有没有在入库时丢掉。**

---

# 5. 还有一个很重要的问题：你写了“系统自动追加参考来源”，但这个文件里没有实现

你现在写：

```text
列表由系统自动追加，你只需在正文用 [1] 形式引用即可
```



但是我看完整个 `build_user_prompt()`：

最后就是：

```python
lines.append("## 你的回答")
```

然后：

```python
return "\n".join(lines)
```

并没有：

```text
## 参考来源
[1] 《xxx》——中华医学会
[2] 《xxx》——健康中国
```



所以这里有一个**很明显的逻辑断层**：

> Prompt 告诉模型“系统会自动追加参考来源”，但是这个文件并没有追加。

如果你在别的 Python 文件里确实有：

```python
append_sources(...)
```

那没问题。

**如果没有，那这就是现在“参考来源不完整”的直接原因。**

---

# 6. 我建议不要让 LLM 自己生成参考来源

这是你这个项目很重要的一点。

现在你的思路有一点：

```text
LLM：
“参考来源”
↓
自己根据上下文生成
```

我不建议。

应该：

```text
Retriever
 ↓
得到 contexts
 ↓
程序生成 sources
 ↓
LLM 只负责正文
 ↓
前端程序负责显示来源
```

也就是：

### LLM输出

```text
减肥过程中，与其追求所谓的“快速减肥方法”，更重要的是建立能够长期坚持的生活方式。[1]

如果你想从饮食方面开始，可以……
```

然后你的 Python 自动生成：

```text
参考来源

[1] 《科学认识体重管理》
    中华医学会
    2026-04-21

[2] 《健康生活方式》
    健康中国
```

这样来源**100%来自数据库**。

模型没有机会把：

> 《某某不存在的文章》

编出来。

这对你的毕业设计来说也是一个很好的技术点：

> **引用信息由检索系统提供，而非由生成模型自主生成，从而降低引用幻觉风险。**

---

# 7. 还有一个我认为你现在必须改的地方：`资料不足`不要交给 LLM 判断

你现在写：

```text
资料不足时明确拒答：
直接说明“知识库暂无与该问题直接相关的资料”
```



这个方向对，但是：

**LLM 自己判断“相关不相关”并不可靠。**

比如用户问：

> 冥想

Chroma 给你：

```text
4-7-8 呼吸法
```

模型可能觉得：

> 呼吸放松 ≈ 冥想

然后又开始胡扯。

这就是你之前截图的问题。

---

# 8. 应该在 RAG 检索层就设置“相关度阈值”

例如：

```text
用户问题
   ↓
Embedding
   ↓
Chroma
   ↓
Top K
   ↓
相关度判断
   ↓
┌───────────────┐
│ 足够相关       │ → LLM正常回答
└───────────────┘
        │
        ↓
┌───────────────┐
│ 不够相关       │ → 直接拒答
└───────────────┘
```

比如：

```python
if not contexts or max_score < threshold:
    return build_rejected_prompt(
        query,
        "知识库中没有找到与该问题直接相关的资料"
    )
```

**具体 threshold 不能现在拍脑袋定。**

因为你的 `score` 到底是：

* cosine similarity
* distance
* Chroma distance
* LlamaIndex similarity

不同配置含义不一样。

所以这个要看你实际 Retriever 代码。

---

# 9. 还有个细节：你现在的 `score` 不应该塞给模型

你现在：

```python
if score:
    header_parts.append(f"相关度：{score:.3f}")
```



这个我建议删掉。

因为：

```text
相关度：0.782
```

对模型没有太大帮助。

而且可能造成模型奇怪的行为：

> “参考资料1相关度最高，因此……”

用户也不需要看到这个。

**相关度应该用于程序判断“要不要回答”，而不是告诉 LLM。**

---

# 10. 你现在的“历史记忆”也进步了

这个我注意到了。

你现在增加了：

```python
user_profile
```

并且加入：

```text
年龄
性别
身高
体重
健康目标
饮食偏好
运动习惯
健康顾虑
已知状况
提及药物
```

同时还有：

```python
conversation_history[-5:]
```



这比之前单纯的对话历史更完整。

不过这里我要提醒你：

### `user_profile` 不应该简单地“全塞给模型”。

例如用户只是问：

> 什么是高血压？

没必要把：

```text
身高：
体重：
运动习惯：
饮食偏好：
……
```

全部给模型。

最好以后做成：

```text
当前问题相关画像
```

例如用户问减肥：

```text
健康目标：减重
饮食偏好：……
运动习惯：……
```

用户问睡眠：

```text
健康顾虑：睡眠
```

这样上下文会更干净。

---

# 11. 还有一个隐藏问题：你的“非医疗问题”判断比较粗

你现在：

```python
NON_MEDICAL_KEYWORDS = [
    "天气", "股票", "基金", ...
    "菜谱", "旅游", "购物", "美食",
]
```



这里：

> 菜谱

其实可能出现：

> 糖尿病患者可以吃什么菜？

这是医疗/营养科普。

如果关键词检测直接把：

> 菜谱

判定成非医疗，就会误拒。

同样：

> 美食

也可能出现：

> 痛风患者能不能吃海鲜？

所以你的**非医疗检测不要只靠关键词**。

最理想的是：

```text
关键词初筛
+
LLM/分类器判断
```

或者至少：

> 只有当问题整体明显属于非医疗领域时才拒答。

---

# 12. 紧急关键词也有点太激进

你这里：

```python
"头晕",
"心跳加速",
"过敏",
"骨折",
```



例如：

> 最近偶尔有点头晕，是怎么回事？

直接：

> 立即拨打120！

用户会被吓死。

这不是你想要的效果。

**“出现某个关键词” ≠ “紧急情况”。**

比如：

> “我昨天运动完有点头晕，今天已经好了。”

显然和：

> “突然头晕并且意识模糊。”

不是一个等级。

所以紧急情况最好从：

```text
关键词触发
```

升级成：

```text
症状 + 程度 + 突发性 + 持续性 + 意识状态
```

进行判断。

至少不要让：

```text
头晕
```

单独触发 120。

---

# 13. 我现在给你这版一个评价

如果上一版我给：

**6/10**

现在我会给：

**8/10。**

你已经把最大的方向改过来了：

### 已经改好的

* ✅ 回答不再强制“论文腔”
* ✅ 不再每句话都引用
* ✅ 资料不足明确拒答
* ✅ 标题已经进入 Prompt
* ✅ 来源、URL、分类、发布时间已经进入 Prompt
* ✅ 增加了用户长期画像
* ✅ 保留了多轮对话
* ✅ 有来源冲突处理
* ✅ 有 Prompt Injection 防御

这些都不错。 

---

# 14. 但现在最值得你改的 5 件事

我建议你不要继续乱加 Prompt 了，**现在应该开始“减法”。**

按优先级：

### 第一优先级 ⭐⭐⭐⭐⭐

**检查 TXT → LlamaIndex → Chroma 的 metadata。**

确保：

```python
{
    "title": "...",
    "source": "...",
    "url": "...",
    "publish_date": "...",
    "category": "..."
}
```

真的进入 Chroma。

---

### 第二优先级 ⭐⭐⭐⭐⭐

**把检索相关度阈值做起来。**

不要让：

> “冥想”

检索到：

> “4-7-8呼吸法”

然后让 LLM 自己发挥。

---

### 第三优先级 ⭐⭐⭐⭐

**把 `QUESTION_TYPE_TEMPLATES` 改成“回答重点”，不要再用 1、2、3、4 的机械模板。**

---

### 第四优先级 ⭐⭐⭐⭐

**参考来源由 Python 程序生成，不让 LLM 自己编。**

最终：

```text
正文：[1]

参考来源
[1] 《文章标题》——中华医学会
```

---

### 第五优先级 ⭐⭐⭐

**重新处理紧急情况和非医疗关键词。**

现在的：

```text
头晕 → 120
菜谱 → 非医疗
```

都太粗糙。

---

## 最关键的一句话

你现在**不要继续改这个 `prompt.py` 了**。

这个文件已经从“问题很大”进入“基本框架正确”的阶段。

**下一步真正该看的，是你 RAG 的“文档加载 / metadata 提取 / Chroma 入库 / Retriever”代码。**

因为现在 Prompt 已经明确要求：

```python
title = meta.get("title", "")
source = meta.get("source", "未知")
url = meta.get("url", "")
publish_date = meta.get("publish_date", "")
category = meta.get("category", "")
```



**如果你前端还是没有标题，那 90% 的问题已经转移到 Prompt 之前的那一段了。**

你把**TXT 导入、LlamaIndex 建 Document、Embedding、Chroma 建库以及检索的代码**发我，我可以直接沿着这条链给你找出“标题到底在哪丢的”，然后把**“自然回答 + [1]引用 + 文章标题 + 来源 + 可点击原文”**这一整套一次性理顺。
